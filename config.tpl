<?php
/**
 * Template for config.php, filled in by backend/writer.php on save (same
 * placeholder-replacement convention as plugins/se_cash-pay/pm_config.tpl).
 * Empty string = no limit, for max_cache_age_days/max_cache_size_mb/max_cache_files.
 */
$imgres_prefs = array(
    "quality" => "{quality}",
    "max_cache_age_days" => "{max_cache_age_days}",
    "max_cache_size_mb" => "{max_cache_size_mb}",
    "max_cache_files" => "{max_cache_files}",
);
